The system's action control; teal = primary, violet = the equally-important secondary path.

```jsx
<Button variant="primary" iconRight={<Icon name="arrow-right" size={16} />}>Commencer le chapitre</Button>
```

Variants: primary, secondary, outline, ghost, danger. Sizes sm/md/lg. Press state is a 1px downward nudge, never a scale.
